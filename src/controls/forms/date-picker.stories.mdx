import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import Form from '../../layout/form'
import DatePicker from './date-picker'

<Meta title="Controls/Forms/DatePicker" component={DatePicker} argTypes={{ onChange: {} }}/>

# DatePicker

The `DatePicker` component allows textual user input in a form.

It adheres to the form grid.

## Baseline

<Canvas>
	<Story name="Baseline" args={{
		label: 'Birthdate',
		gridSpan: 10,
	}}>
		{args => <Form>
			<DatePicker range={false} {...args}/>
		</Form>}
	</Story>
	<Story name="Range" args={{
		startLabel: 'Arrival',
		endLabel: 'Departure',
		gridSpan: 10,
	}}>
		{args => <Form>
			<DatePicker range={true} {...args}/>
		</Form>}
	</Story>
</Canvas>

<ArgsTable story="Baseline"/>
