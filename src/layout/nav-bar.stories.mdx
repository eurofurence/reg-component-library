import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import NavBar from '../layout/nav-bar'

<Meta title="Layout/NavBar" component={NavBar}/>

# NavBar

The NavBar component provides a section at the top of a page to contain branding and navigation elements.
It currently does not expose any parameters.

The NavBar component wraps itself in a HTML `header` tag.

## Baseline

<Canvas>
	<Story name="Baseline">
		<NavBar/>
	</Story>
</Canvas>
