import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import NavBar from '../surfaces/nav-bar'

<Meta title="Surfaces/NavBar" component={NavBar}/>

# NavBar

The NavBar component provides a surface at the top of a page to contain branding and navigation elements.
It currently does not expose any parameters.

The NavBar component wraps itself in a HTML `header` tag.

## Baseline

<Canvas>
	<Story name="Baseline">
		<NavBar/>
	</Story>
</Canvas>
