import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import Form from '../../layout/form'
import FieldSet from './field-set'
import Checkbox from './checkbox'

<Meta title="Controls/Forms/FieldSet" component={FieldSet} argTypes={{ onChange: {} }}/>

# FieldSet

The `FieldSet` component can be used to group a set of radio buttons and/or checkboxes together.

This component adheres to the `Form` grid.

## Baseline

<Canvas>
	<Story name="Baseline" args={{
		legend: 'Pizza toppings',
		gridSpan: 10,
	}}>
		{args => <Form>
			<FieldSet {...args}>
				<Checkbox label="Tomato sauce" />
				<Checkbox label="Mozzarella cheese" />
				<Checkbox label="Paprika" />
				<Checkbox label="Pepperoni" />
				<Checkbox label="Anchovies" />
				<Checkbox label="Olives" />
			</FieldSet>
		</Form>}
	</Story>
</Canvas>

<ArgsTable story="Baseline"/>
